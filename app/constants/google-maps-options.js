export const mapOptions = {
    // Coordinates for the center of the map (e.g., Moscow)
    center: { lat: 55.751244, lng: 37.618423 },
    // Initial zoom level (higher values zoom in closer)
    zoom: 10,
    // Map type: ROADMAP, SATELLITE, HYBRID, or TERRAIN
    // Whether to disable the default UI controls
    disableDefaultUI: false,
    // Additional optional controls (uncomment as needed)
    // zoomControl: true,
    // scaleControl: true,
    // streetViewControl: true,
    // mapTypeControl: true,
    // rotateControl: true,
    // fullscreenControl: true,
  };